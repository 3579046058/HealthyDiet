# AGENTS.md — 项目开发规则

## 核心原则（强制遵守）

### 1. 绝对禁止模拟数据糊弄用户
- **永远不要用 `Math.random()`、`mock`、`simulate`、占位随机值来替代真实功能**
- 如果某项功能需要真实数据/算法/API 但当前不可用：
  - 明确在 UI 显示"暂不可用/未接入"等诚实状态
  - 绝不返回随机/虚假结果让用户误以为功能正常
- 识别类功能（如食物识别）必须返回**真实识别结果**；识别失败就如实告知失败原因，绝不返回随机猜测

### 2. 诚实报告
- 功能没实现就直说"没实现"
- API 不可用就说明原因（SDK 限制、权限、硬件等）
- 不要用"看起来能用"的假数据掩盖问题

### 3. 真实优先
- 能用真实 API 就用真实 API
- 需要用户配置的（如 API Key、签名、权限）明确告知用户操作步骤
- 优先做真实可用的最小功能，而不是"有 UI 没逻辑"的空壳

## 技术约定
- ArkTS 严格模式：无 any/unknown/as 断言，对象字面量需显式接口
- 构建后用 arkts_check 校验
