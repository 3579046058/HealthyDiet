# 健康饮食应用 - 程序架构文档

## 📋 目录结构概览

```
HealthyDiet/
├── AppScope/                    # 应用全局配置
│   └── app.json5               # 应用配置（bundleName、版本等）
├── entry/                       # 主模块
│   ├── src/main/
│   │   ├── ets/                # ArkTS源码
│   │   │   ├── common/         # 公共模块
│   │   │   ├── components/     # UI组件
│   │   │   ├── controller/     # 控制器
│   │   │   ├── entryability/   # 应用入口
│   │   │   ├── model/          # 数据模型
│   │   │   ├── pages/          # 页面
│   │   │   └── services/       # 业务服务
│   │   └── resources/          # 资源文件
│   └── module.json5            # 模块配置
└── hvigor/                      # 构建工具配置
```

---

## 🏗️ 架构设计模式

### 整体架构：MVVM + 服务层模式

```
┌─────────────────────────────────────────────────────────┐
│                     View Layer (页面)                    │
│  Pages: Index, DietPage, SleepPage, SportPage...       │
│  Components: HealthCard, BarChart, RingProgress...     │
└────────────────────┬────────────────────────────────────┘
                     │ @State/@Prop/@Link
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  ViewModel Layer (状态管理)              │
│  GlobalData: 全局状态管理                                │
│  @StorageLink/@StorageProp: 跨组件状态共享               │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  Service Layer (业务逻辑)                │
│  PedometerService, HealthScoreUtil, ChatService...     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│                   Model Layer (数据层)                   │
│  DatabaseHelper: 数据库操作                              │
│  PreferenceHelper: 偏好设置                              │
│  RecycleBinHelper: 回收站                                │
└─────────────────────────────────────────────────────────┘
```

---

## 📂 详细模块说明

### 1. Common 模块 (`common/`)

公共工具类和常量定义，被所有模块共享。

#### 文件列表
```
common/
├── Constants.ets          # 常量定义（颜色、默认值、配置）
├── GlobalData.ets         # 全局数据管理器
└── SleepOverlap.ets       # 睡眠重叠处理工具
```

#### 核心类说明

**Constants.ets** - 应用常量配置（475行）
- 数据库配置：DB_NAME, DB_VERSION
- 默认目标值：步数、饮水、睡眠等
- 颜色主题定义
- 数据接口定义：DbRow, GoalConfig, StreakData等

**GlobalData.ets** - 全局状态管理（30行）
- 数据库实例管理
- 偏好设置实例管理
- 应用上下文管理
- 初始化控制

---

### 2. Model 模块 (`model/`)

数据持久化层，负责数据的存储和读取。

#### 文件列表
```
model/
├── DatabaseHelper.ets      # 数据库操作核心类（1012行）
├── PreferenceHelper.ets    # 用户偏好设置管理（424行）
├── RecycleBinHelper.ets    # 回收站功能
├── AchievementHelper.ets   # 成就系统数据管理
├── ChatModels.ets          # AI对话数据模型
└── ChatPersist.ets         # AI对话持久化
```

#### 数据库表结构

DatabaseHelper 管理以下数据表：
- `daily_summary` - 每日汇总数据
- `weight_records` - 体重记录
- `height_records` - 身高记录
- `water_records` - 饮水记录
- `sport_records` - 运动记录
- `sleep_records` - 睡眠记录
- `heart_rate_records` - 心率记录
- `step_records` - 步数记录
- `diet_records` - 饮食记录

#### 核心方法

**DatabaseHelper 主要方法**：
- `init()` - 初始化数据库
- `saveDailySummary()` - 保存每日汇总
- `getDailySummary()` - 获取每日数据
- `saveWaterRecord()` - 保存饮水记录
- `saveSleepRecord()` - 保存睡眠记录
- `saveSportRecord()` - 保存运动记录

**PreferenceHelper 主要方法**：
- `getGoalConfig()` - 获取目标配置
- `saveGoalConfig()` - 保存目标配置
- `getStreakData()` - 获取连续打卡数据
- `getTheme()` - 获取主题设置
- `saveTheme()` - 保存主题设置

---

### 3. Services 模块 (`services/`)

业务逻辑层，处理复杂的业务操作和系统集成。

#### 文件列表（15个服务）
```
services/
├── PedometerService.ets          # 计步器服务（211行）
├── HealthScoreUtil.ets           # 健康评分工具
├── ChatService.ets               # AI对话服务
├── FoodRecognitionService.ets    # 食物识别服务
├── FoodRecognizer.ets            # 食物识别器
├── ReminderScheduler.ets         # 提醒调度器
├── NotificationService.ets       # 通知服务
├── GoalNotificationHelper.ets    # 目标通知助手
├── PermissionService.ets         # 权限管理服务
├── SoundEffectService.ets        # 音效服务
├── VoiceInputService.ets         # 语音输入服务
├── BackgroundTaskService.ets     # 后台任务服务
├── LocationTrackerService.ets    # 位置追踪服务
├── CardDataHelper.ets            # 卡片数据助手
└── KbService.ets                 # 知识库服务
```

#### 核心服务说明

**PedometerService** - 计步器服务（单例模式）
- 计步器传感器监听
- 加速度计备用方案
- 步数实时同步到数据库
- 支持传感器模式切换

**HealthScoreUtil** - 健康评分算法
- 多维度加权评分（步数、睡眠、饮水、运动、饮食）
- 水桶加权算法（短板效应）
- 评分范围：-100 到 100
- 权重配置：各项权重合计80分

**ChatService** - AI对话服务
- 消息发送与接收
- 对话历史管理
- AI接口集成

**ReminderScheduler** - 提醒调度器
- 创建/取消提醒
- 系统通知集成
- 提醒时间管理

---

### 4. Components 模块 (`components/`)

UI组件层，可复用的界面组件。

#### 文件列表（16个组件）
```
components/
├── HealthCard.ets            # 健康数据卡片
├── RingProgress.ets          # 圆环进度组件
├── NestedRings.ets           # 嵌套圆环图
├── BarChart.ets              # 柱状图组件
├── TrendChart.ets            # 趋势图组件
├── ScoreTrendChart.ets       # 评分趋势图
├── AnimatedNumber.ets        # 数字动画组件
├── FoodItemCard.ets          # 食物卡片
├── FoodRecognitionTab.ets    # 食物识别标签页
├── ChatTab.ets               # AI对话标签页
├── DiaryTab.ets              # 日记标签页
├── HealthTab.ets             # 健康标签页
├── AccountTab.ets            # 账号标签页
├── AchievementDialog.ets     # 成就弹窗
├── SharePanel.ets            # 分享面板
└── OverlayDialogs.ets        # 覆盖层对话框
```

#### 核心组件说明

**HealthCard** - 健康数据卡片
- 显示健康数据（步数、饮水、睡眠等）
- 支持数字动画效果
- 主题适配
- 点击交互

**RingProgress** - 圆环进度组件
- Canvas绘制圆环
- 进度动画
- 自定义颜色和尺寸

**BarChart** - 柱状图组件
- Canvas绘制柱状图
- 营养成分可视化
- 动态数据更新

**NestedRings** - 嵌套圆环图
- 多维度数据展示
- 健康评分可视化
- 交互式图表

---

### 5. Pages 模块 (`pages/`)

页面层，应用的各个界面。

#### 文件列表（共18个页面）
```
pages/
├── Index.ets                  # 主页（核心入口，53666行）
├── DietPage.ets               # 饮食页面（21595行）
├── SportPage.ets              # 运动页面
├── SleepPage.ets              # 睡眠页面
├── WaterPage.ets              # 饮水页面
├── WeightPage.ets             # 体重页面
├── MoodPage.ets               # 情绪页面（16897行）
├── MedicationPage.ets         # 用药页面（18263行）
├── WomenHealthPage.ets        # 女性健康页面
├── ReportPage.ets             # 报告页面
├── GoalPage.ets               # 目标设置页面（4269行）
├── SettingsPage.ets           # 设置页面
├── NotificationPage.ets       # 通知管理页面（18705行）
├── LoginPage.ets              # 登录页面（11322行）
├── ProfilePage.ets            # 个人资料页面（7180行）
├── PermissionPage.ets         # 权限管理页面（6301行）
├── RecycleBinPage.ets         # 回收站页面
├── MakeupDaysPage.ets         # 补签页面（10520行）
└── SoundSettingsPage.ets      # 音效设置页面
```

#### 核心页面说明

**Index.ets** - 主页（应用核心）
- 5个标签页：健康概览、饮食记录、AI对话、日记、账号
- 健康评分展示（嵌套圆环图）
- 健康数据卡片网格
- 快捷功能入口
- 数据实时更新

**DietPage.ets** - 饮食页面
- 餐次选择（早餐、午餐、晚餐、加餐）
- 食物信息库浏览
- 食物搜索和分类
- 营养成分统计
- 柱状图可视化

**SportPage.ets** - 运动页面
- 运动类型选择
- 运动时长记录
- 卡路里消耗计算
- 运动历史查看

**SleepPage.ets** - 睡眠页面
- 入睡/起床时间记录
- 睡眠质量评分
- 睡眠趋势图表
- 睡眠建议

**WaterPage.ets** - 饮水页面
- 饮水量记录
- 饮水目标追踪
- 饮水提醒设置
- 饮水历史统计

**WeightPage.ets** - 体重页面
- 体重数据录入
- BMI计算
- 体重趋势图
- 目标体重设置

**MoodPage.ets** - 情绪页面
- 情绪类型选择
- 情绪日记记录
- 情绪趋势分析
- 情绪建议

**MedicationPage.ets** - 用药页面
- 药品信息管理
- 服药提醒设置
- 用药记录查看
- 药品库存管理

---

### 6. Controller 模块 (`controller/`)

控制器层，处理复杂的交互逻辑。

#### 文件列表
```
controller/
└── ChatController.ets    # AI对话控制器
```

**ChatController.ets** - AI对话控制器
- 处理用户输入
- 管理对话状态
- 调用ChatService
- 更新UI状态

---

### 7. EntryAbility 模块 (`entryability/`)

应用入口，负责应用生命周期管理。

#### 文件列表
```
entryability/
└── EntryAbility.ets    # 应用入口类
```

**EntryAbility.ets** - 应用入口
```typescript
class EntryAbility extends UIAbility {
  // 应用创建时初始化
  onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    // 初始化GlobalData
    // 初始化各种服务
    // 设置窗口属性
  }
  
  // 应用销毁时清理
  onDestroy(): void {
    // 停止计步器服务
    // 清理资源
  }
  
  // 前台切换
  onForeground(): void {
    // 恢复数据更新
  }
  
  // 后台切换
  onBackground(): void {
    // 暂停部分更新
  }
}
```

初始化的服务：
- GlobalData：全局数据管理
- PedometerService：计步器服务
- PermissionService：权限服务
- ReminderScheduler：提醒调度
- FoodRecognitionService：食物识别
- SoundEffectService：音效服务
- CardDataHelper：卡片数据
- ChatService：AI对话
- GoalNotificationHelper：目标通知
- BackgroundTaskService：后台任务

---

## 🔄 数据流与生命周期

### 数据流向

```
用户操作 (UI事件)
    ↓
Pages/Components (View层)
    ↓
Services (业务逻辑处理)
    ↓
Model (数据持久化)
    ↓
Database/Preferences (存储层)
```

### 页面生命周期

```
aboutToAppear()
    ↓
初始化数据
    ↓
加载用户偏好
    ↓
查询数据库
    ↓
更新UI状态
    ↓
用户交互
    ↓
aboutToDisappear()
    ↓
保存数据
    ↓
清理资源
```

### 状态管理流程

```
@State (组件内状态)
    ↓
@Prop (父子组件传递)
    ↓
@Link (双向绑定)
    ↓
@StorageLink (全局状态)
    ↓
GlobalData (应用级状态)
```

---

## ⚙️ 配置文件说明

### 1. app.json5 - 应用配置
```json5
{
  "app": {
    "bundleName": "com.yourcompany.healthapp",
    "vendor": {
      "name": "YourCompany",
      "description": "健康管理应用"
    },
    "versionCode": 2000000,
    "versionName": "2.0.0",
    "icon": "$media:app_icon",
    "label": "$string:app_name"
  }
}
```

### 2. module.json5 - 模块配置
```json5
{
  "module": {
    "name": "entry",
    "type": "entry",
    "requestPermissions": [
      {
        "name": "ohos.permission.INTERNET"
      },
      {
        "name": "ohos.permission.MICROPHONE"
      },
      {
        "name": "ohos.permission.PUBLISH_AGENT_REMINDER"
      },
      {
        "name": "ohos.permission.KEEP_BACKGROUND_RUNNING"
      }
    ],
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "description": "健康饮食应用",
        "icon": "$media:icon",
        "label": "$string:entry_label",
        "startWindowIcon": "$media:startIcon",
        "startWindowBackground": "$color:start_window_background"
      }
    ]
  }
}
```

### 3. main_pages.json - 页面路由配置
```json
{
  "src": [
    "pages/Index",
    "pages/WeightPage",
    "pages/WaterPage",
    "pages/SportPage",
    "pages/SleepPage",
    "pages/ReportPage",
    "pages/GoalPage",
    "pages/DietPage",
    "pages/WomenHealthPage",
    "pages/MedicationPage",
    "pages/MoodPage",
    "pages/LoginPage",
    "pages/NotificationPage",
    "pages/ProfilePage",
    "pages/SettingsPage",
    "pages/PermissionPage",
    "pages/RecycleBinPage",
    "pages/MakeupDaysPage",
    "pages/SoundSettingsPage"
  ]
}
```

### 4. build-profile.json5 - 构建配置
```json5
{
  "app": {
    "signingConfigs": [],
    "compileSdkVersion": 9,
    "compatibleSdkVersion": 9,
    "products": [
      {
        "name": "default",
        "signingConfig": "default"
      }
    ]
  },
  "modules": [
    {
      "name": "entry",
      "srcPath": "./entry",
      "targets": [
        {
          "name": "default",
          "applyToProducts": [
            "default"
          ]
        }
      ]
    }
  ]
}
```

---

## 🔐 权限管理

### 申请的权限

| 权限名称 | 用途 | 必需 |
|---------|------|------|
| ohos.permission.INTERNET | 网络访问（AI功能） | 是 |
| ohos.permission.MICROPHONE | 语音输入 | 否 |
| ohos.permission.PUBLISH_AGENT_REMINDER | 系统提醒通知 | 是 |
| ohos.permission.KEEP_BACKGROUND_RUNNING | 后台运行（计步） | 是 |
| ohos.permission.CAMERA | 相机（食物识别） | 否 |

### 权限申请流程

```
PermissionService
    ↓
检查权限状态
    ↓
未授权 → 申请权限
    ↓
用户授权/拒绝
    ↓
更新权限状态
    ↓
功能可用/不可用
```

---

## 🎨 主题系统

### 主题配置

```typescript
interface ThemeColors {
  // 浅色主题
  background: string;
  cardBackground: string;
  text: string;
  textSecondary: string;
  divider: string;
  
  // 深色主题
  darkBackground: string;
  darkCardBackground: string;
  darkText: string;
  darkTextSecondary: string;
  darkDivider: string;
}
```

### 主题切换流程

```
用户切换主题
    ↓
PreferenceHelper.saveTheme()
    ↓
AppStorage.setOrCreate('currentTheme')
    ↓
@StorageLink 触发更新
    ↓
所有组件重新渲染
```

---

## 📊 数据库设计

### 表结构设计

**daily_summary 表**（每日汇总）
```sql
CREATE TABLE daily_summary (
  date TEXT PRIMARY KEY,
  steps INTEGER,
  water_ml INTEGER,
  sleep_hours REAL,
  exercise_minutes INTEGER,
  health_score INTEGER,
  created_at INTEGER,
  updated_at INTEGER
)
```

**weight_records 表**（体重记录）
```sql
CREATE TABLE weight_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT,
  weight_kg REAL,
  bmi REAL,
  created_at INTEGER
)
```

**water_records 表**（饮水记录）
```sql
CREATE TABLE water_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT,
  amount_ml INTEGER,
  time TEXT,
  created_at INTEGER
)
```

**sport_records 表**（运动记录）
```sql
CREATE TABLE sport_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT,
  type TEXT,
  duration_minutes INTEGER,
  calories INTEGER,
  created_at INTEGER
)
```

**sleep_records 表**（睡眠记录）
```sql
CREATE TABLE sleep_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT,
  start_time TEXT,
  end_time TEXT,
  duration_hours REAL,
  quality_score INTEGER,
  created_at INTEGER
)
```

**diet_records 表**（饮食记录）
```sql
CREATE TABLE diet_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT,
  meal_type TEXT,
  food_name TEXT,
  amount_grams INTEGER,
  calories INTEGER,
  protein REAL,
  fat REAL,
  carbs REAL,
  created_at INTEGER
)
```

---

## 🚀 性能优化策略

### 1. 数据加载优化
- **懒加载**：按需加载数据，减少初始加载时间
- **分页查询**：历史数据分页加载
- **缓存机制**：常用数据缓存在内存

### 2. UI渲染优化
- **Canvas绘制**：图表使用Canvas，减少组件嵌套
- **虚拟列表**：长列表使用LazyForEach
- **防抖节流**：频繁操作使用防抖节流

### 3. 数据库优化
- **索引优化**：为常用查询字段建立索引
- **批量操作**：批量插入减少IO次数
- **异步操作**：数据库操作异步执行

### 4. 内存优化
- **及时释放**：页面销毁时释放资源
- **避免泄漏**：注意闭包和回调的引用
- **图片优化**：使用合适尺寸的图片

---

## 🔧 扩展性设计

### 1. 模块化设计
- 各模块职责单一，耦合度低
- 通过接口通信，易于替换实现
- 新功能可独立添加模块

### 2. 配置化设计
- 目标值可配置
- 权重可配置
- 主题可配置
- 卡片顺序可配置

### 3. 插件化设计
- AI服务可替换
- 图表组件可扩展
- 数据源可扩展

---

## 📈 架构优势

### 1. 清晰的分层
- View、Service、Model职责明确
- 代码易于理解和维护
- 便于团队协作开发

### 2. 高复用性
- 组件化设计，UI组件可复用
- 服务层封装，业务逻辑可复用
- 工具类统一管理

### 3. 易测试性
- 业务逻辑与UI分离
- 服务层可独立测试
- 数据层可模拟测试

### 4. 可扩展性
- 新功能易于添加
- 架构支持横向扩展
- 配置化设计灵活

### 5. 高性能
- 数据懒加载
- Canvas绘制优化
- 异步操作不阻塞UI

---

## 🎯 最佳实践

### 1. 状态管理
- 合理使用@State、@Prop、@Link
- 全局状态使用@StorageLink
- 避免过度使用全局状态

### 2. 数据操作
- 异步操作使用async/await
- 错误处理使用try-catch
- 数据验证在Service层

### 3. 组件设计
- 单一职责原则
- Props验证
- 事件向上传递

### 4. 性能优化
- 避免不必要的渲染
- 使用LazyForEach处理长列表
- 及时清理资源

---

## 📝 开发规范

### 命名规范
- 文件名：PascalCase（如 HealthCard.ets）
- 类名：PascalCase（如 DatabaseHelper）
- 方法名：camelCase（如 saveDailySummary）
- 常量：UPPER_SNAKE_CASE（如 DB_NAME）

### 注释规范
- 类注释：说明类的职责
- 方法注释：说明参数、返回值、作用
- 复杂逻辑：添加行内注释

### 代码组织
- 导入语句统一管理
- 常量定义在文件顶部
- 私有方法放在公共方法之后

---

**总结**：该架构采用MVVM + 服务层模式，分层清晰，职责明确，具有良好的可维护性、可扩展性和高性能。通过合理的模块划分、状态管理和性能优化，为用户提供流畅的使用体验。
